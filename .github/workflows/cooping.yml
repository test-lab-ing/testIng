# # 1. ตั้งชื่อหุ่นยนต์ (อะไรก็ได้)
# name: My Learning Robot

# # 2. จะให้หุ่นยนต์ทำงานตอนไหน? (จุดนี้ต้องสอดคล้องกับชื่อกิ่ง)
# on:
#   push:
#     branches: [ "main", "add-ing" ]  # ใส่ชื่อกิ่งที่คุณใช้บ่อยๆ ลงไปที่นี่
#   pull_request:
#     branches: [ "main" ]            # ให้ทำงานตอนที่มีคนทำ PR เข้าหา main

# # 3. สั่งให้หุ่นยนต์ทำอะไรบ้าง?
# jobs:
#   check-everything:                 # ชื่อกลุ่มงาน
#     runs-on: ubuntu-latest          # ใช้คอมจำลองของ GitHub

#     steps:                          # ลำดับขั้นตอน (ต้องเคาะเว้นวรรคให้ตรง)
#       - name: 1. ก๊อปปี้ไฟล์ในเครื่องเราไปให้หุ่นยนต์
#         uses: actions/checkout@v4

#       - name: 2. ให้หุ่นยนต์รายงานตัว
#         run: echo "สวัสดีครับ ผมคือหุ่นยนต์ที่คุณเขียนเองกับมือ!"

#       - name: 3. ลองเช็คดูว่าในโปรเจกต์มีไฟล์อะไรบ้าง
#         run: ls -R                  # คำสั่ง Linux สำหรับลิสต์ไฟล์ทั้งหมด

#       - name: 4. ลองเช็คว่าโค้ด (ถ้ามี) รันผ่านไหม
#         run: echo "กำลังตรวจสอบโค้ดของคุณ... ทุกอย่างดูดีมาก!"